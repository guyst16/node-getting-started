build:
  hello-world:
    context: .

deploy:
  - echo "OKTETO_BUILD_HELLO_WORLD_IMAGE=${OKTETO_BUILD_HELLO_WORLD_IMAGE}"
  - envsubst < k8s.yml | kubectl apply -f -

dev:
  hello-world-node:
    command: bash
    sync:
      - .:/usr/src/app
    forward:
      - 9229:9229
